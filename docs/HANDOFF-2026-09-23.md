# 黑光树洞开发交接 — 2026-09-23

## 当前结论

开发环境已有可用的真实文字发布、回应、人工审核/申诉与 PostgreSQL 图片处理链路；尚未达到上线验收。用户本轮要求保存进度、编写交接、推送 GitHub。不要把本次推送理解为合并 main、正式发布或验收全部通过。

前端仓库：`Muwei-TA/tdesign-miniprogram-starter`。后端仓库：`Muwei-TA/blacklight`。两端当前集成分支均为 `codex/cloudbase-integration`。主要开发提交截至交接前：前端 `f8bddb4`、后端 `9691340`；交接提交本身及最新远端 SHA 用 `git log -1` / `git ls-remote` 确认。

## 工作区与授权

- 实际前端：`/Users/muwei/WeChatProjects/blacklight-development/frontend`。
- 实际后端：`/Users/muwei/WeChatProjects/blacklight-development/backend`。
- 原 `/Users/muwei/WeChatProjects/shudong` 仍是官方 TDesign 模板且包含用户原有改动，未覆盖、未提交到官方上游。评估文件在该目录 `blacklight-review-2026-09-22.md`。
- 已按 save-my-astra 进行隔离 Git worktree 并行开发；功能已集成到上述两条分支，各专题分支仍保留。使用 `git worktree list` 查看，勿删除未核对的工作区。
- 用户已选择适配现有 PostgreSQL，未授权新建付费环境；已明确授权真实调试微信账号“牧维”为首位 moderator，初始化有审计。
- 本轮明确授权把当前修改推送 GitHub；没有正式发布小程序的授权。

## 运行环境

- CloudBase：`shudong-d4g4blap4a5069a28`，上海；AppID `wx39773ed34aa30776`。
- PostgreSQL：现有实例，12 项迁移已应用。业务表/RPC 仅服务端使用；`anon/authenticated` 不可直连业务对象。
- 已部署 `api` 和 `worker`，运行时 Node.js 20.19。视频关闭，`review-callback` 未部署，不需要为当前同步图片审核创建回调通道。
- PG 原生私有桶 `blacklight-private`，2 MiB，JPEG；RLS 无客户端放行策略。不要改回旧 COS/wx-server-sdk 存储通道。
- `publishing/uploads=true`；`publicScope/video/anthology/export=false`。
- API 凭据只在云端环境变量：`CLOUDBASE_APIKEY`、`ANON_ALIAS_SECRET`、`MINIPROGRAM_APP_ID`。不读取/复制其值到聊天或 Git；开发 API Key 2026-10-22 到期，正式运营前需轮换方案。
- CLS 已开通。旧 GetFunctionLogs 接口不可用；查 CLS 时避免输出正文、OpenID、匿名映射、签名 URL 和凭据。

## 已实现与已验证

- 真实 `wx.cloud.callFunction` 会话；每次调用验证当前微信身份上下文，防止热实例残留身份；伪造 payload 用户信息无效。
- PostgreSQL 事务实现发帖与审核 outbox、并发幂等、评论、入社、管理决定、申诉、注销及图片绑定；8 次同键并发发帖只有一组副作用。
- 内容安全失败不放行。前台本人提交后通过受限内部 runner 复用 worker 的租约和原子完成 RPC；外部没有“设为审核通过”的接口。
- 真实页面文字发布为 published，原生输入回应已 published，详情显示回应计数 1；管理员退回→作者申诉→管理员批准已实测（批准申诉重新排队，不直接公开）。
- 图片解码/尺寸检查/移除 EXIF/重编码 JPEG，审核的是实际展示字节；真实测试图上传 PG 并通过 imgSecCheck，状态 verified。匿名图文帖已 published，作者 userId/displayName 不返回。
- 媒体每次授权读取重新签名，链接 TTL 300 秒；管理员无权读取他人私密手记。私密帖本人保存为 private_saved，管理调用伪造 owner 仍 not_accessible。
- 后端 111 项测试通过，前端 8 个测试文件通过，两端 lint/check 通过。前端注册 22 页面；后端 56 actions。这些不能替代完整多角色和真机验收。
- 详细证据：前端 `docs/evidence/acceptance-20260923.json`、`published-result.jpg`、`published-comment.jpg`；两端 `docs/cloudbase-acceptance.md` / `docs/cloudbase-development.md`。

## 首要未完成项

1. **图片下载域名**：真实 `wx.getImageInfo` 返回 `url not in domain list`。已请用户在公众平台服务器域名把 `https://shudong-d4g4blap4a5069a28.api.tcloudbasegateway.com` 加入 downloadFile 合法域名，尚无完成回执。当前浏览器安全策略禁止访问 mp.weixin.qq.com，不得绕过。不能关闭 urlCheck 冒充修复。配置完成后刷新项目信息并实际复测图片读取/预览。
2. **后台审核 token**：真实 API 微信会话 msgSecCheck/imgSecCheck 通过；独立 SCF timer 的 OpenAPI 仍报 `-501001 invalid wx openapi access_token`。定时器运行本身已验证；审核失败保持 pending，最终转 manual。前台审核可用不等于后台审核已修复。
3. **最近未验证的触发器操作**：后端 `cloudfunctions/worker/config.json` 已补上与 cloudbaserc 相同的 `blacklight-review-minute` / `0 */1 * * * * *`，并同步前端本地部署副本。在开发者工具对该 config 右键看到了“上传触发器”，使用 Home/Return 后菜单关闭，但没有成功提示或云端差异证据。必须先核对当前触发器状态，不得声称上传成功，也不要盲目再创建同名函数。
4. **用量护栏 T-B09**：只有现有单用户日上传 20 MiB/并发上传意图限制。可配置社团总量与审核配额告警尚未完成。`backend-usage-guards` / `codex/usage-guards` 工作树只创建，无代码改动；子代理因用量上限停止。下一位可从任务书和现有 `20260922183000_atomic_images.sql` 新建后续迁移，不修改已应用迁移。
5. **任务书逐项审计尚未完成**：`docs/11-task-board.md` 仍保留原基线“待领取”文字。以实际代码和验收记录为准，但要逐项回填状态，不能仅凭页面存在标已验收。重点复核退回后重新编辑/重提、头像上传、错误态、访客/成员 A/B/被移除成员矩阵、清理故障恢复。
6. **真机和运营**：未完成两个真实账号与 iOS/Android 验收、备份恢复演练、费用/积压告警、主体/类目/备案等 G0 后台核验、运营值守和正式提审。公开、视频、文集、导出继续关闭。

## 继续开发命令与注意事项

后端：`npm ci && npm run sync && npm test && npm run lint && npm run check`。
前端：`npm ci && npm test && npm run lint && npm run check`。
SQL 集成用例在后端 `tests/integration/`，运行时使用事务回滚；先确认开发环境。新迁移须 dry-run、实际应用、查询远端记录，不能把本地文件存在当成部署成功。

前端本地云函数副本由 `node scripts/prepare-cloudfunctions.mjs ../backend` 生成，被 Git 与小程序打包排除；后端是唯一源码。部署改动前先 sync。已有函数在微信工具应“同步云函数列表”后“上传并部署”，不要选择“创建并部署”，否则 ResourceInUse.FunctionName。OpenAPI 权限配置需要微信工具登记；单纯 MCP 更新代码不保证登记权限。

当前微信开发者工具项目为 `heiguang-shudong`（frontend），不是旧模板“黑光树洞”。CLI 需使用：

```sh
ELECTRON_BASE_APP_PATH=/Applications/wechatwebdevtools.app/Contents/Resources/app.asar \
wechatide -c Codex <工具名> --project /Users/muwei/WeChatProjects/blacklight-development/frontend
```

已在本会话授权 CLI；不得为了诊断读取访问令牌。自动化 callMethod/evaluate 参数用 `--args-file`，不要用废弃 `--args`。原生 AX 的右键菜单点击曾失败，键盘操作后也必须核对实际结果。

开发预览码本地路径：`/Users/muwei/WeChatProjects/blacklight-development/preview-20260923.jpg`。预览包总 1,371,384 字节、主包 1,166,399 字节。二维码可能过期，可重新生成；未把二维码推送到仓库，也未正式上传/发布。

## 接手顺序

先检查两个集成分支最新 HEAD 与工作区，阅读两端当前验收文档；核对用户是否已配置域名。随后确认触发器操作结果，继续后台 token 排障；并在隔离分支补齐 T-B09、逐项任务书审计。任何“通过”都需对应实际证据，不可绕过鉴权、内容检查或域名校验来获得表面成功。
